{
  "name": "lab-acherlab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npx vite build",
    "start:backend": "cd backend && node src/index.js",
    "start:frontend": "cd frontend && npx vite preview --port 5173",
    "dev": "cd backend && node src/index.js & cd frontend && npx vite --port 5173",
    "start": "cd backend && node src/index.js & cd frontend && npx vite preview --port 5173 --host 0.0.0.0"
  }
}
